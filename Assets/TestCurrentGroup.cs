using UnityEngine;
using System.Collections;

public class TestCurrentGroup : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log (DBValues.CurrentGroup.Nombre);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
